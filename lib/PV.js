//PV.js is offered by Ben F Rayfield under opensource MIT license.
//Its a high dimensional sparse vector system as jsonObs.
//A dagball game state is an infinite dimensional scalar-field contains infinite variety of scalar-fields to mix by weighted-sum, each defined by json + streaming numbers. http://namespace.alice.py is position of y in alice's ball. .vy is its velocity. .pi/.vi are influence/exists.
const PV = {
	mapToId: map=>{
		return 'TODO_mapToId_similarToDagverseButDifferentDatastruct';
	},
};
console.log('PV = '+PV);